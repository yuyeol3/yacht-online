spring:
  datasource:
    url: jdbc:mysql://localhost:3311/database
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update


jwt:
  secret: ${JWT_SECRET}